class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count=0
        for i in nums:
            if len(str(i)) % 2 == 0:
                count=count+1
        return count
        count=0
        
class Solution: 
    def findNumbers(self, nums: List[int]) -> int:
        count=0
        for i in range (len(nums)):
            if nums[i]>=10 and nums[i]<=99:
                count=count+1
            elif nums[i]>=1000 and nums[i]<=9999:
                count=count+1
            elif nums[i]==100000:
                count=count+1
        return count
        
        # Strings seems to be slightly better for speed and memory usage in this problem #
       
